--- a/yaak/src-tauri/src/window.rs
+++ b/yaak/src-tauri/src/window.rs
@@ -198,7 +198,7 @@
     let config = CreateWindowConfig {
         url,
         label: label.as_str(),
-        title: "Yaak",
+        title: "Fiak",
         inner_size: Some((DEFAULT_WINDOW_WIDTH, DEFAULT_WINDOW_HEIGHT)),
         position: Some((
             // Offset by random amount so it's easier to differentiate
--- a/yaak/src-web/commands/openSettings.tsx
+++ b/yaak/src-web/commands/openSettings.tsx
@@ -20,7 +20,7 @@
     await invokeCmd('cmd_new_child_window', {
       url: location.href,
       label: 'settings',
-      title: 'Yaak Settings',
+      title: 'Fiak Settings',
       innerSize: [750, 600],
     });
   },
--- a/yaak/src-web/components/Settings/SettingsGeneral.tsx
+++ b/yaak/src-web/components/Settings/SettingsGeneral.tsx
@@ -68,7 +68,7 @@
           className="pl-2 mt-1 ml-[14rem]"
           checked={settings.autoDownloadUpdates}
           disabled={!settings.autoupdate}
-          help="Automatically download Yaak updates (!50MB) in the background, so they will be immediately ready to install."
+          help="Automatically download Fiak updates (!50MB) in the background, so they will be immediately ready to install."
           title="Automatically download updates"
           onChange={(autoDownloadUpdates) => patchModel(settings, { autoDownloadUpdates })}
         />
--- a/yaak/src-web/components/Settings/SettingsPlugins.tsx
+++ b/yaak/src-web/components/Settings/SettingsPlugins.tsx
@@ -276,7 +276,7 @@
   return plugins.length === 0 ? (
     <div className="pb-4">
       <EmptyStateText className="text-center">
-        Plugins extend the functionality of Yaak.
+        Plugins extend the functionality of Fiak.
         <br />
         Add your first plugin to get started.
       </EmptyStateText>
--- a/yaak/src-web/components/SettingsDropdown.tsx
+++ b/yaak/src-web/components/SettingsDropdown.tsx
@@ -63,7 +63,7 @@
           leftSlot: <Icon icon="rocket" />,
           onSelect: () => openUrl('https://yaak.app/button/new'),
         },
-        { type: 'separator', label: `Yaak v${appInfo.version}` },
+        { type: 'separator', label: `Fiak v${appInfo.version}` },
         {
           label: 'Purchase License',
           color: 'success',
--- a/yaak/src-web/components/git/GitDropdown.tsx
+++ b/yaak/src-web/components/git/GitDropdown.tsx
@@ -400,7 +400,7 @@
             const confirmed = await showConfirm({
               id: 'hide-git-init-prompt',
               title: 'Hide Git Setup',
-              description: 'You can initialize a git repo outside of Yaak to bring this back',
+              description: 'You can initialize a git repo outside of Fiak to bring this back',
             });
             if (confirmed) {
               await setHidden((prev) => ({ ...prev, [workspaceId]: true }));
--- a/yaak/src-web/components/responseViewers/WebPageViewer.tsx
+++ b/yaak/src-web/components/responseViewers/WebPageViewer.tsx
@@ -21,7 +21,7 @@
     <div className="h-full pb-3">
       <iframe
         key={body ? 'has-body' : 'no-body'}
-        title="Yaak response preview"
+        title="Fiak response preview"
         srcDoc={contentForIframe}
         sandbox="allow-scripts allow-forms"
         referrerPolicy="no-referrer"
--- a/yaak/src-web/hooks/useSyncWorkspaceRequestTitle.ts
+++ b/yaak/src-web/hooks/useSyncWorkspaceRequestTitle.ts
@@ -16,7 +16,7 @@
 
   useEffect(() => {
     const settings = jotaiStore.get(settingsAtom);
-    let newTitle = activeWorkspace ? activeWorkspace.name : 'Yaak';
+    let newTitle = activeWorkspace ? activeWorkspace.name : 'Fiak';
     if (activeEnvironment) {
       newTitle += ` (${activeEnvironment.name})`;
     }
--- a/yaak/src-web/index.html
+++ b/yaak/src-web/index.html
@@ -3,7 +3,7 @@
 <head>
   <meta charset="UTF-8"/>
   <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
-  <title>Yaak App</title>
+  <title>Fiak App</title>
 <!--  <script src="http://localhost:8097"></script>-->
 
   <!-- Certain elements like webview (and maybe <select>?) will use background
--- a/yaak/src-web/lib/initGlobalListeners.tsx
+++ b/yaak/src-web/lib/initGlobalListeners.tsx
@@ -53,7 +53,7 @@
       timeout: null,
       message: (
         <VStack>
-          <h2 className="font-semibold">Yaak {version} was installed</h2>
+          <h2 className="font-semibold">Fiak {version} was installed</h2>
           <p className="text-text-subtle text-sm">Start using the new version now?</p>
         </VStack>
       ),
@@ -68,7 +68,7 @@
             setTimeout(() => invokeCmd('cmd_restart', {}), 200);
           }}
         >
-          Relaunch Yaak
+          Relaunch Fiak
         </ButtonInfiniteLoading>
       ),
     });
@@ -90,7 +90,7 @@
         timeout: null,
         message: (
           <VStack>
-            <h2 className="font-semibold">Yaak {version} is available</h2>
+            <h2 className="font-semibold">Fiak {version} is available</h2>
             <p className="text-text-subtle text-sm">
               {downloaded ? 'Do you want to install' : 'Download and install'} the update?
             </p>
--- a/yaak/src-tauri/tauri.conf.json
+++ b/yaak/src-tauri/tauri.conf.json
@@ -1,7 +1,7 @@
 {
-  "productName": "Yaak",
+  "productName": "Fiak",
   "version": "0.0.0",
-  "identifier": "app.yaak.desktop",
+  "identifier": "app.fiak.desktop",
   "build": {
     "beforeBuildCommand": "npm run tauri-before-build",
     "beforeDevCommand": "npm run tauri-before-dev",
--- a/yaak/src-tauri/tauri.development.conf.json
+++ b/yaak/src-tauri/tauri.development.conf.json
@@ -1,6 +1,6 @@
 {
-  "productName": "Daak",
-  "identifier": "app.yaak.desktop.dev",
+  "productName": "Faak",
+  "identifier": "app.fiak.desktop.dev",
   "bundle": {
     "externalBin": [
       "vendored/node/yaaknode",
--- a/yaak/src-tauri/tauri.linux.conf.json
+++ b/yaak/src-tauri/tauri.linux.conf.json
@@ -1,3 +1,3 @@
 {
-  "productName": "yaak"
+  "productName": "fiak"
 }
--- a/yaak/src-tauri/tauri.release.conf.json
+++ b/yaak/src-tauri/tauri.release.conf.json
@@ -13,10 +13,10 @@
     }
   },
   "bundle": {
-    "publisher": "Yaak",
+    "publisher": "Fiak",
     "license": "MIT",
-    "copyright": "Yaak",
-    "homepage": "https://yaak.app",
+    "copyright": "Fiak",
+    "homepage": "https://github.com/Vexcited/Fiak",
     "active": true,
     "category": "DeveloperTool",
     "createUpdaterArtifacts": true,
