--- a/yaak/src-web/commands/openSettings.tsx
+++ b/yaak/src-web/commands/openSettings.tsx
@@ -23,7 +23,7 @@
     await invokeCmd('cmd_new_child_window', {
       url: location.href,
       label: 'settings',
-      title: 'Yaak Settings',
+      title: 'Fiak Settings',
       innerSize: [750, 600],
     });
   },
--- a/yaak/src-web/components/Settings/SettingsGeneral.tsx
+++ b/yaak/src-web/components/Settings/SettingsGeneral.tsx
@@ -72,7 +72,7 @@
           className="pl-2 mt-1 ml-[14rem]"
           checked={settings.autoDownloadUpdates}
           disabled={!settings.autoupdate}
-          help="Automatically download Yaak updates (!50MB) in the background, so they will be immediately ready to install."
+          help="Automatically download Fiak updates (!50MB) in the background, so they will be immediately ready to install."
           title="Automatically download updates"
           onChange={(autoDownloadUpdates) => patchModel(settings, { autoDownloadUpdates })}
         />
--- a/yaak/src-web/components/Settings/SettingsPlugins.tsx
+++ b/yaak/src-web/components/Settings/SettingsPlugins.tsx
@@ -345,7 +345,7 @@
   return plugins.length === 0 ? (
     <div className="pb-4">
       <EmptyStateText className="text-center">
-        Plugins extend the functionality of Yaak.
+        Plugins extend the functionality of Fiak.
         <br />
         Add your first plugin to get started.
       </EmptyStateText>
--- a/yaak/src-web/components/SettingsDropdown.tsx
+++ b/yaak/src-web/components/SettingsDropdown.tsx
@@ -63,7 +63,7 @@
           leftSlot: <Icon icon="rocket" />,
           onSelect: () => openUrl('https://yaak.app/button/new'),
         },
-        { type: 'separator', label: `Yaak v${appInfo.version}` },
+        { type: 'separator', label: `Fiak v${appInfo.version}` },
         {
           label: 'Purchase License',
           color: 'success',
--- a/yaak/src-web/components/git/GitDropdown.tsx
+++ b/yaak/src-web/components/git/GitDropdown.tsx
@@ -430,7 +430,7 @@
             const confirmed = await showConfirm({
               id: 'hide-git-init-prompt',
               title: 'Hide Git Setup',
-              description: 'You can initialize a git repo outside of Yaak to bring this back',
+              description: 'You can initialize a git repo outside of Fiak to bring this back',
             });
             if (confirmed) {
               await setHidden((prev) => ({ ...prev, [workspaceId]: true }));
--- a/yaak/src-web/hooks/useSyncWorkspaceRequestTitle.ts
+++ b/yaak/src-web/hooks/useSyncWorkspaceRequestTitle.ts
@@ -16,7 +16,7 @@
 
   useEffect(() => {
     const settings = jotaiStore.get(settingsAtom);
-    let newTitle = activeWorkspace ? activeWorkspace.name : 'Yaak';
+    let newTitle = activeWorkspace ? activeWorkspace.name : 'Fiak';
     if (activeEnvironment) {
       newTitle += ` (${activeEnvironment.name})`;
     }
--- a/yaak/src-web/index.html
+++ b/yaak/src-web/index.html
@@ -3,7 +3,7 @@
 <head>
   <meta charset="UTF-8"/>
   <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
-  <title>Yaak App</title>
+  <title>Fiak App</title>
 <!--  <script src="http://localhost:8097"></script>-->
 
   <!-- Certain elements like webview (and maybe <select>?) will use background
--- a/yaak/crates-tauri/yaak-app/tauri.release.conf.json
+++ b/yaak/crates-tauri/yaak-app/tauri.release.conf.json
@@ -30,10 +30,10 @@
     }
   },
   "bundle": {
-    "publisher": "Yaak",
+    "publisher": "Fiak",
     "license": "MIT",
-    "copyright": "Yaak",
-    "homepage": "https://yaak.app",
+    "copyright": "Fiak",
+    "homepage": "https://github.com/Vexcited/Fiak",
     "active": true,
     "category": "DeveloperTool",
     "createUpdaterArtifacts": true,
--- a/yaak/crates/yaak-models/src/queries/workspaces.rs
+++ b/yaak/crates/yaak-models/src/queries/workspaces.rs
@@ -19,7 +19,7 @@
         if workspaces.is_empty() {
             workspaces.push(self.upsert_workspace(
                 &Workspace {
-                    name: "Yaak".to_string(),
+                    name: "Fiak".to_string(),
                     setting_follow_redirects: true,
                     setting_validate_certificates: true,
                     ..Default::default()
